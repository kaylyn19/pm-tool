<% @project.each do |project| %>
    <% if params[:tag] %>
        <span>Products with <p class="badge badge-light"><%= params[:tag] %></p></span>
    <%end%>

    <h1><%= link_to project.title, project_path(project.id) %></h1>
    <p><%= project.description %></p>
    <hr>
<% end %>